version: "3"

services:
  trifler_web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: trifler_web
    restart: always
    volumes:
      - ./:/usr/src/app
    ports:
      - "3000:3000"
